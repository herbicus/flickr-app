describe('flickrService', function() {

  beforeEach(module('flickrModule'));

  it('should ...', inject(function(flickrService) {

    //expect(flickrService.doSomething()).toEqual('something');

  }));

});
